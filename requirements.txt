# HTTP y scraping
httpx>=0.27.0
curl_cffi>=0.7.0
playwright>=1.40.0
playwright-stealth>=1.0.6
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Base de datos
pyodbc>=5.0.0
sqlalchemy>=2.0.0

# Scheduler
apscheduler>=3.10.0

# UI
pystray>=0.19.0
Pillow>=10.0.0
textual>=0.50.0

# Config y seguridad
pydantic-settings>=2.0.0
cryptography>=42.0.0

# Logging
loguru>=0.7.0

# Email
# smtplib viene con Python stdlib

# CAPTCHA (solo Getnet)
2captcha-python>=1.1.3

# OTP
pyotp>=2.9.0      # TOTP automático (Fiserv)
pyzbar>=0.1.9     # lectura de QR codes
zxing-cpp>=2.2.0  # lectura de QR — tools/extractor.py y el botón "Subir QR" del panel
# imaplib viene con Python stdlib

# Resolución de reCAPTCHA por audio (Fiserv — sin pagar)
SpeechRecognition>=3.10.0   # Google Speech API gratuita (fallback offline)
pydub>=0.25.1               # conversión mp3→wav para SpeechRecognition
# openai-whisper>=20231117  # recomendado: offline, sin límites de API
#                           # pip install openai-whisper
#                           # Requiere ffmpeg en PATH (dev) o empaquetado en .exe
# ffmpeg NO va en requirements.txt — va en tools/ffmpeg/ y se empaqueta con PyInstaller

# Empaquetado (solo para build)
pyinstaller>=6.0.0

